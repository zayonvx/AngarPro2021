const initialState = {
  popup: {
    visible: false,
    children: null,
    closeable: false,
    buttonBackVisible: false,
    buttonForwardVisible: true,
    buttonSendVisible: false,
  },
};

export default initialState;
